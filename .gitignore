# dependencies (bun install)
node_modules

# Trigger.dev local dev state
.trigger
**/.trigger

# output
out
dist
*.tgz

# code coverage
coverage
*.lcov

# logs
logs
*.log
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# Claude Code tooling state
.claude
.claude-flow

# Eval run outputs (ai package)
.eval-runs
**/.eval-runs

# Eval triage analysis artifacts (ai package) — regenerable, large
**/evals/.triage

# Ledger of the pages the last eval run built, so the next one sweeps exactly
# those (evals/cases/pages.ts). Machine-local: a shared copy would name rows
# another machine's database has never heard of.
**/evals/.eval-pages.json

# Python bytecode caches (sandbox-assets SDK, bundled skill scripts, sdk-templates)
__pycache__/
*.pyc