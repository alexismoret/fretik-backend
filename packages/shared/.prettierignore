# Generated by app/page-runtime/scripts/sync-theme-tokens.ts and hashed as-is
# (the hash beside it is the page recompile key). Reformatting it makes the
# next `page-runtime:build` fail format:check for nobody's benefit.
src/services/pages/compile-assets/theme-tokens.css
