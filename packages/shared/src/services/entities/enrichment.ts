/**
 * Placeholder for future entity enrichment pipeline.
 * Called when an entity is confirmed or created.
 *
 * The future implementation should:
 *   - Receive entity name + type
 *   - Search the internet for company info via an AI agent
 *   - Persist: website, address, country, phone, email, validated type
 */
export const triggerEntityEnrichment = (_entityId: string) => {
  // No-op for now.
};
