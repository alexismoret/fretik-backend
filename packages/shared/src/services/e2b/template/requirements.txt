# Sandbox worker image — Python deps pre-installed so code_interpreter-style
# execution works offline (network_mode=none blocks pip at runtime).
# This file is the ONLY source of truth: `e2b/template/build.ts` reads it and
# nothing else does. Changing it requires a `bun run e2b:build` to take effect.

# Data frames & arrays
pandas==3.0.2
numpy==2.4.4
pyarrow==23.0.1

# String matching (fuzzy joins across files with formatting differences)
rapidfuzz==3.14.5

# Excel readers / writers
openpyxl==3.1.5
xlrd==2.0.2
xlsxwriter==3.2.9

# Office docs
python-docx==1.2.0
python-pptx==1.0.2
# Safe XML parsing. The pptx skill mandates it for OOXML transforms:
# round-tripping through `xml.etree.ElementTree` rewrites namespace
# prefixes and corrupts the deck.
defusedxml==0.7.1
# OpenDocument reader, so pandas can open a .ods the way it opens an .xlsx.
odfpy==1.4.1

# Outlook .msg reader. The backend already extracts a mail's headers and
# body to markdown; this is what lets the agent go further in the sandbox —
# unpack the attachments, and open a .msg nested inside another .msg.
extract-msg==0.56.1
compressed-rtf==1.0.7
ebcdic==2.0.1
olefile==0.47
red-black-tree-mod==1.22
RTFDE==0.1.2.2
tzlocal==5.4.4
beautifulsoup4==4.15.0
soupsieve==2.9.2
# Markdown extraction from Office / PDF files. Used by the pptx and
# pdf skills to read content without parsing XML manually.
markitdown==0.1.5

# OCR for scanned PDFs — the pdf skill prescribes both by name. `pytesseract`
# is only a wrapper around the `tesseract` binary, installed via apt in
# build.ts; `pdf2image` wraps `pdftoppm`, already there via poppler-utils.
pytesseract==0.3.13
pdf2image==1.17.0

# PDF parsing & generation
pypdf==6.11.0
pdfplumber==0.11.9
pdfminer.six==20251230
pypdfium2==5.7.0
reportlab==4.5.1

# Plotting / images
matplotlib==3.10.8
pillow==12.2.0
contourpy==1.3.3
cycler==0.12.1
fonttools==4.62.1
kiwisolver==1.5.0

# Science
scipy==1.17.1
scikit-learn==1.8.0
statsmodels==0.14.6
joblib==1.5.3
patsy==1.0.2
threadpoolctl==3.6.0

# Shared transitive pins
cffi==2.0.0
charset-normalizer==3.4.7
cryptography==46.0.7
et_xmlfile==2.0.0
lxml==6.1.0
packaging==26.1
pycparser==3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0
typing_extensions==4.15.0

# Note: ipykernel / jupyter-client / pyzmq / traitlets are NOT pinned here
# because they ship in the upstream `code-interpreter-v1` base image — that's
# what runs the persistent Jupyter kernel daemon (port 49999) we hit via
# `sbx.runCode({ context })` in run-in-sandbox.ts. Re-pinning them would risk
# version drift with the base image; let upstream own them.
